# Fixed Cloudflare Pages `staging` branch only.
VITE_DEPLOY_ENV=staging
VITE_API_URL=https://journal-backend-staging.chired.workers.dev

# Replace with the dedicated staging Google web OAuth client ID.
# It must differ from the production client.
VITE_GOOGLE_CLIENT_ID=123456789012-replace-with-staging-client.apps.googleusercontent.com
