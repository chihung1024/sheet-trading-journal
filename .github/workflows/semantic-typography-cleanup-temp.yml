name: Semantic Typography Cleanup Temporary

on:
  push:
    branches:
      - feat/phase13-2-typography-density-convergence

permissions:
  contents: write

concurrency:
  group: semantic-typography-cleanup-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cleanup-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: feat/phase13-2-typography-density-convergence
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '22'
          cache: npm

      - name: Install pinned frontend dependencies
        run: npm ci --no-audit --no-fund

      - name: Apply semantic typography cleanup
        run: node tools/semantic_typography_cleanup.mjs

      - name: Run complete frontend contract suite
        run: npm run test:frontend

      - name: Build frontend
        run: npm run build

      - name: Commit cleanup
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No semantic cleanup changes remain."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- src
          git commit -m "refactor: normalize semantic typography roles"
          git push origin HEAD:feat/phase13-2-typography-density-convergence
