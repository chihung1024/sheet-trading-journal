name = "journal-backend"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 綁定 D1 資料庫
[[d1_databases]]
binding = "DB"
database_name = "journal-db"
database_id = "fbef989c-dede-4f94-8907-6a2134cdf372"

# 環境變數設定 (建議在 Cloudflare Dashboard 設定 Secrets，這裡僅為預留)
[vars]
GITHUB_OWNER = "chihung1024"
GITHUB_REPO = "sheet-trading-journal"
# API_SECRET = (請在 Dashboard 設定)
# GITHUB_TOKEN = (請在 Dashboard 設定)
# ADMIN_EMAIL = (請在 Dashboard 設定)
